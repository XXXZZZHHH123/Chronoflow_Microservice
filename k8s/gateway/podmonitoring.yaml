apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: gateway
  namespace: microservices
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
